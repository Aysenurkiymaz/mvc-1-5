<%- include('../partials/navigation') %>
<h1>Books</h1>
<% if (books.length === 0) { %>
  <p>No books has been found</p>
<% } else { %>
  <ul>
    <% books.forEach(book => { %>
      <li><%= book.title %> by <%= book.author.name %></li>
    <% }); %>
  </ul>
<% } %>